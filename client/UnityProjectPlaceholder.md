# Unity Project Placeholder

The real Unity project will be created later under:

```text
client/UnityProject/
```

This placeholder exists to document the target location without committing Unity-generated project files during Phase 1 Task 2.

When the Unity project is created, keep generated folders such as `Library/`, `Temp/`, `Obj/`, `Logs/`, and build outputs out of Git.

